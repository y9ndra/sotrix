apiVersion: 1

providers:
  - name: "Sotrix Dashboards"
    orgId: 1
    folder: "Sotrix"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/definitions
